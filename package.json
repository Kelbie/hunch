{
  "name": "hunch-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc -p packages/core && tsc -p packages/cli && tsc -p apps/github-app",
    "build": "bun run --cwd packages/core build && bun run --cwd packages/cli build",
    "check": "bun run typecheck && bun test && bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "22",
    "esbuild": "0.25.12",
    "tsup": "8.5.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "bun@1.3.5"
}
