# expect: correctness/edge-case-regression
--- a/src/limit.py
+++ b/src/limit.py
@@ -1,3 +1,2 @@
 def limit(value):
-    # Zero means no results; only a missing limit uses 20.
-    return 20 if value is None else value
+    return value or 20
