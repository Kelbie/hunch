# expect: rust/error-context
--- a/src/retry.rs
+++ b/src/retry.rs
@@ -1,9 +1,6 @@
 fn read() -> Result<Data, LoadError> {
-    load().map_err(|e| match e.kind() {
-        ErrorKind::TimedOut => LoadError::Retry,
-        _ => LoadError::Permanent,
-    })
+    load().map_err(|_| LoadError::Permanent)
 }
 fn attempt() {
     if matches!(read(), Err(LoadError::Retry)) { enqueue_retry(); }
 }
